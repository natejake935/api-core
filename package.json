{
  "name": "contractor-lead-api",
  "version": "1.0.0",
  "private": true,
  "description": "Shared lead capture API — handles email + SMS (Twilio) and CRM (HubSpot). Configure per customer via .env.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "~2.8.5",
    "dotenv": "~16.4.5",
    "express": "~4.19.2",
    "express-rate-limit": "~7.3.1",
    "helmet": "~7.1.0",
    "twilio": "~5.3.0"
  }
}
